import {corpus} from '@/lib/engine';export function GET(){return Response.json({id:'demo-creator',content:corpus.filter(x=>x.category==='workplace'),rankingFormula:'0.35 category affinity + 0.30 semantic relevance + 0.20 predicted performance + 0.15 novelty'})}
