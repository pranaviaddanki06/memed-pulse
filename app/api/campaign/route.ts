import {analyze} from '@/lib/engine';export async function POST(req:Request){try{const x=await req.json(),captions=[`A relatable ${x.tone||'bold'} moment for ${x.brand||'your brand'}`,`The conversation ${x.brand||'this campaign'} starts today`,`A small moment with a big cultural signal`];return Response.json({concepts:captions.map(c=>({caption:c,...analyze({caption:c,category:'relatable',audience:x.audience||'Gen Z',objective:x.objective})}))})}catch{return Response.json({error:'Invalid campaign request'},{status:400})}}
