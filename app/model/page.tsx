import {metrics} from '@/lib/engine';export default function Model(){const f=[['Cultural relevance',21],['Emotional intensity',21],['Semantic novelty',17],['Alignment',16],['Audience fit',13],['Brand suitability',12]];return <main className="page"><header className="pagehead"><span className="eyebrow">Technical transparency</span><h1>Model Intelligence</h1><p>A reproducible prototype trained/evaluated on correlated synthetic demonstration data.</p></header><section className="kpis"><div className="kpi panel"><span>Feature count</span><strong>{metrics.features}</strong></div><div className="kpi panel"><span>Training samples</span><strong>{metrics.samples}</strong></div><div className="kpi panel"><span>Model MAE</span><strong>{metrics.mae}</strong></div><div className="kpi panel"><span>Baseline MAE</span><strong>{metrics.baselineMae}</strong></div></section><section className="grid two"><div className="panel"><h2>Feature Importance</h2>{f.map(x=><div className="barrow" key={x[0]}><span>{x[0]}</span><div className="bar"><i style={{width:`${x[1]*4}%`}}/></div><b>{x[1]}%</b></div>)}</div><div className="panel"><h2>Evaluation</h2><p className="muted">Model: {metrics.model}</p><p className="muted">Holdout MAE {metrics.mae}; RMSE {metrics.rmse}; R² {metrics.r2}.</p><p className="muted">Mean baseline MAE {metrics.baselineMae}; RMSE {metrics.baselineRmse}; R² {metrics.baselineR2}.</p><p className="notice">Metrics describe the synthetic prototype target, not real-world view prediction.</p></div></section></main>}
