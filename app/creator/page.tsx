import {corpus} from '@/lib/engine';export default function Creator(){const c=corpus.filter(x=>x.category==='workplace');const avg=Math.round(c.reduce((a,x)=>a+x.performance,0)/c.length);return <main className="page"><header className="pagehead"><span className="eyebrow">Demo creator profile</span><h1>Creator Intelligence</h1><p>Transparent ranking of what to post next.</p></header><section className="kpis"><div className="kpi panel"><span>Total content</span><strong>{c.length}</strong></div><div className="kpi panel"><span>Average attention</span><strong>{avg}</strong></div><div className="kpi panel"><span>Strongest category</span><strong>Work</strong></div><div className="kpi panel"><span>Audience affinity</span><strong>84</strong></div></section><section className="panel" style={{marginTop:13}}><h2>What should this creator post next?</h2><div className="cards">{['Meeting survival guides','Workplace nostalgia carousel','Manager POV reaction'].map((x,i)=><article className="card" key={x}><span className="tag">RANK {i+1} · {89-i*4}</span><strong>{x}</strong><p className="muted">Ranking = 0.35 category affinity + 0.30 semantic relevance + 0.20 predicted performance + 0.15 novelty.</p></article>)}</div></section></main>}
