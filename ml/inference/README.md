# Inference

`lib/engine.ts` is the deployed inference adapter. It is stateless and serverless-safe. The optional sklearn artifact can be served from a dedicated Python service when replacing the demo adapter.
